spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://stockoneq.ca0qidoxblxf.ap-northeast-2.rds.amazonaws.com:3306/stockoneq?serverTimezone=UTC
    username: admin
    password: stockoneq!

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
        format_sql: true
        default_batch_fetch_size: 50

  config:
    import:
      - classpath:secret.yml

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error